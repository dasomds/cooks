<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="kr.co.cooks.dao.UserDao">

	<select id="existUser" parameterType="Map" resultType="user">
		select ID, PASSWORD, NAME, ADDRESS, PHONE, USER_LEVEL, MILEAGE from USERS where ID = #{id} AND password=#{password}
	</select>
	

</mapper>

